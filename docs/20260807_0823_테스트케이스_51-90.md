# 테스트케이스 51~90 (2026-08-06 추가분)

**작성 일시: 2026-08-07 08:23**

> 기존 `테스트케이스_50개.md`(1~50번, 재료 변경/RAG 적정성/재료명 정규화/선택·캐싱 중심)에 이어서,
> 오늘 추가된 기능(요리 이름 검색, RAG 출처 표기, 정규화 불량 데이터 정리, 배포) 기준으로 51번부터 이어서 작성.
>
> 각 행 앞에 표시:
> - **[챗봇]** — 실제 챗봇 화면/API에 직접 쳐서 확인해야 하는 것 (LLM 호출 발생, 사용자가 직접 실행)
> - **[확인]** — LLM 호출 없이 코드/파일/로그만 보면 되는 것 ("이거 확인해줘"라고 부탁하면 Claude가 바로 실행 가능)

---

## F. 요리 이름 검색 — 분류 및 RAG 매칭

| # | 구분 | 입력 (사용자 발화) / 확인 대상 | 상황 | 예상 동작 |
|---|---|---|---|---|
| 51 | [챗봇] | "김치찌개 레시피 알려줘" | 첫 턴 | `query_type="레시피 이름 검색"`, `dish_name="김치찌개"`로 정확히 분류 |
| 52 | [챗봇] | "계란이랑 밥 있는데 뭐 해먹을까" | 첫 턴 | 이름 검색이 아니라 **레시피 추천**으로 분류 (재료 나열과 오분류 안 되는지) |
| 53 | [챗봇] | "계란볶음밥 만드는 법 알려줘" | 첫 턴 | 이름 검색 분류 + `dish_name="계란볶음밥"` (부제 없이 정확히 추출) |
| 54 | [챗봇] | "볶음밥 레시피 알려줘" | RAG에 동명이레시피 다수 존재 | 매칭된 후보가 여러 개면 1개가 아니라 **리스트**로 전부 노출 |
| 55 | [챗봇] | "된장찌개 레시피" | RAG에 동명이레시피 다수 존재 | 54번과 동일하게 여러 개 노출, 각 옵션 재료 목록이 비어있지 않은지 |
| 56 | [챗봇] | "쫄면 레시피 알려줘" | 오늘 재료 없는 쫄면 2건(3751516, 3944213) 삭제 완료 | 정상적으로 재료 채워진 쫄면 결과만 노출 (이미 1차 확인됨 — 재확인용) |
| 57 | [챗봇] | (54/55/56 결과에서) "1번" 선택 | 이름 검색 결과가 여러 개인 상태 | 재생성 없이 캐시로 바로 레시피 본문(재료+조리순서) 확정 |
| 58 | [챗봇] | "쏘야볶 알려줘" | 축약어 입력 | 이름 검색으로 분류되는지, `dish_name`을 어떻게 추출하는지 관찰 (정답 기준 없음, 참고용) |
| 59 | [챗봇] | "우주에서 만드는 라면" 같이 RAG에 없을 존재 요리 이름 | RAG 매칭 0건 예상 | `judge_name_match`가 빈 리스트 반환 → `generate_recipe_by_name`으로 **딱 1개만** 생성 |
| 60 | [챗봇] | 59번 결과 레시피 확인 | 생성된 레시피 | 실제로 사람이 먹을 수 있는 정상 요리인지(괴식 아님), 30분 이내 조리 가능한지, title이 요청한 이름 그대로인지 |

---

## G. 요리 이름 검색 — 궁합 경고 / 분류 회귀

| # | 구분 | 입력 (사용자 발화) / 확인 대상 | 상황 | 예상 동작 |
|---|---|---|---|---|
| 61 | [챗봇] | 매운 재료 레시피 A와 유제품 재료 레시피 B가 동시에 매칭될 만한 이름으로 검색 | 이름 검색 결과 2개 이상 | 궁합 경고가 **레시피별로 따로 계산**돼서, A·B 재료를 섞은 오탐(예: A의 고추+B의 우유) 경고가 뜨지 않는지 |
| 62 | [챗봇] | "매콤한 돼지고기 요리 추천해줘" (이름 아닌 재료/특성 나열) | 첫 턴 | 이름 검색이 아니라 레시피 추천으로 분류되는지 (회귀 확인) |
| 63 | [챗봇] | "카레볶음밥 알려줘" | RAG에 존재 | 이름 검색 정상 분류 + 매칭, 정규화 불량 없이 재료 정상 노출 |
| 64 | [챗봇] | "돈까스 레시피" (오늘 삭제된 불량 돈까스 계열 3건 존재했던 이름) | 삭제 후 상태 | 삭제된 불량 항목 대신 정상 돈까스 결과만 노출 |
| 65 | [챗봇] | "족발 레시피" (오늘 삭제된 불량 족발 계열 존재했던 이름) | 삭제 후 상태 | 64번과 동일 패턴 확인 |
| 66 | [챗봇] | 이름 검색 진행 중 재료 언급이 섞인 애매한 발화 (예: "집에 있는 재료로 만들 수 있는 김치찌개 알려줘") | 경계 케이스 | 이름 검색/재료 추천 중 어느 쪽으로 판정되는지 관찰, 크래시 없이 처리되는지 |
| 67 | [챗봇] | 이름 검색 결과 선택 후 "다른 이름으로 다시 검색해줘" | 대화 중간 | `recipe_options` 리셋되고 새 이름 검색 흐름이 정상적으로 다시 시작되는지 |
| 68 | [챗봇] | 이름 검색 → 생성 폴백 → 생성된 레시피 선택 | 59/60번 이어서 | `finalize_recipe` 캐시 히트로 재생성 없이 바로 확정되는지 (사전 캐시 채움 확인) |
| 69 | [챗봇] | 이름 검색으로 안내 메시지 문구 확인 | 여러 개 매칭된 경우 | "요청하신 요리를 찾았어요..." 등 문구가 어색하지 않은지 (여러 개인데 단수형 문구면 UX 이슈로 보고) |
| 70 | [챗봇] | 이름 검색 결과 0개(생성도 실패하는 극단 케이스 유도 어려우면 스킵) | LLM 생성 실패 상황 | fallback 메시지("찾지 못했어요")로 안전하게 처리되는지 |

---

## H. RAG 출처 표기 + 정규화 데이터 정리 검증

| # | 구분 | 입력 (사용자 발화) / 확인 대상 | 상황 | 예상 동작 |
|---|---|---|---|---|
| 71 | [챗봇] | 재료 기반 추천 흐름에서 RAG 옵션 선택 → 확정 | 기존 흐름 | 답변 끝에 `출처: 만개의레시피 (https://www.10000recipe.com/recipe/{id})` 정확한 형식으로 표기 |
| 72 | [챗봇] | 이름 검색에서 RAG 매칭 옵션 선택 → 확정 | 신규 흐름 | 71번과 동일한 출처 문구 확인 |
| 73 | [챗봇] | LLM 생성(`source="generated"`) 레시피 확정 | 재료 추천이든 이름 검색이든 | 출처 문구가 **붙지 않는지** (생성 레시피는 출처 없어야 정상) |
| 74 | [확인] | `data_pipeline/structured_recipes/*.json` 전수 스캔 | - | title/ingredients/steps 전부 채워져 있는지 (불량 0건인지 재확인) |
| 75 | [확인] | 로컬 Chroma VDB(`data/vdb`) 문서 수 | - | 정확히 992개, orphan(로컬 JSON에 없는 doc_id) 0건인지 |
| 76 | [확인] | S3 `vdb.version` 값과 로컬 `data/vdb.version` 값 비교 | - | 두 값이 일치하는지 (마지막 업로드 시점 기준) |
| 77 | [챗봇] | 재료 추천 흐름에서 예전엔 재료 0개였던 RAG 문서가 나올 법한 재료 조합 입력 | 회귀 확인 | `evaluate_rag_recipe`가 N=0을 여전히 "부적합"으로 걸러내는지 (기존 로직 안 건드렸지만 확인) |
| 78 | [확인] | `_is_valid_recipe` 함수 단위 테스트 | title/ingredients/steps 각각 하나씩 비운 경우 | 셋 다 False, 정상 케이스만 True (파이썬 pure function, LLM 없음) |
| 79 | [확인] | 삭제된 60개 recipe_id 목록과 현재 `structured_recipes` 파일 목록 대조 | - | 60개 전부 실제로 없는지 (완전 삭제 확인) |
| 80 | [챗봇] | "곰탕 레시피" (오늘 삭제된 불량 곰탕 계열 존재했던 이름) | 삭제 후 상태 | 정상 곰탕 결과만 노출, 크래시 없음 |

---

## I. EC2 배포 확인 (배포 완료 후 진행)

| # | 구분 | 입력 (사용자 발화) / 확인 대상 | 상황 | 예상 동작 |
|---|---|---|---|---|
| 81 | [확인] | GitHub Actions 배포 워크플로우 실행 결과 | 방금 푸시한 배포 | build-and-push, deploy 두 job 모두 성공(초록색)인지 |
| 82 | [확인] | EC2 컨테이너 상태 (`docker ps`) | 배포 직후 | `chatbot-container`가 `Up` 상태로 떠 있는지 |
| 83 | [확인] | EC2 컨테이너 부팅 로그 | 배포 직후 | `sync_vdb_from_s3` 관련 로그(다운로드 or 스킵) 및 VDB 문서 수(992개) 확인 |
| 84 | [챗봇] | 배포된 서버 주소로 "/" 접속 | 배포 직후 | 정상적으로 페이지 로드되는지 |
| 85 | [챗봇] | 배포된 서버에서 재료 나열 → 레시피 추천 흐름 1회 | 배포 직후 스모크 테스트 | 기존 핵심 흐름이 배포 환경에서도 정상 동작하는지 |
| 86 | [챗봇] | 배포된 서버에서 "쫄면 레시피 알려줘" | 배포 직후 | 로컬과 동일하게 정규화 불량 없이 정상 결과 노출 (S3가 이미 정리된 상태이므로) |
| 87 | [챗봇] | 배포된 서버에서 RAG 매칭 확정 후 출처 표기 확인 | 배포 직후 | 71번과 동일하게 출처 문구 정상 표기 |
| 88 | [확인] | EC2 호스트 `/home/ubuntu/data/` 디렉토리 구조 | 배포 직후 | 넓어진 볼륨 마운트로 `vdb/`와 (다음 배포부터 생길) `vdb.version`이 같은 곳에 있는지 |
| 89 | [확인] | EC2 디스크 사용량 (`docker system df`, `df -h`) | 배포 직후 | 이전 트러블슈팅에서 다룬 CUDA/이미지 누적 문제 재발 없는지 |
| 90 | [챗봇] | 배포된 서버에서 SSE 스트리밍(`/query`) progress 메시지 확인 | 배포 직후 | `filter_valid_candidates`(검색 결과를 정리하는 중) 등 신규 노드 문구가 정상 노출되는지 |

---

## 참고

- **[확인]** 항목은 API 비용 없이 파일/DB/로그만 보면 되는 것들이라, "이 중에서 OO 확인해줘"라고 말씀해주시면 바로 실행해드립니다.
- **[챗봇]** 항목은 전부 LLM 호출이 발생하므로 직접 실행해주셔야 합니다 (기존 규칙 그대로).
- I 카테고리는 배포가 완전히 끝난 뒤에 순서대로(81 → 82 → 83 → 84~90) 진행하는 걸 권장합니다.
