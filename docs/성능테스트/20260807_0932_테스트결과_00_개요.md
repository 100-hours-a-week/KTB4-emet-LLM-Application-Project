# 테스트 결과 종합 (2026-08-06~07)

**테스트 일시: 2026-08-07 08:23 ~ 09:32**

`테스트케이스_전체_90개.md`의 A~E 섹터(1~50번, 기존 재료 변경/RAG 적정성/재료명 정규화/선택·캐싱)와,
오늘 새로 추가된 F 섹터(재요청 시 중복 방지 기능, 91~100번)를 실제로 실행한 결과 모음입니다.

섹터별 상세 결과는 각각 별도 파일로 분리했습니다 (분량이 많아 하나로 합치기 어려움).

## 파일 목록

| 파일 | 내용 |
|---|---|
| [테스트결과_A_재료변경판단.md](테스트결과_A_재료변경판단.md) | 1~10번, 재료 initial/add/remove/replace 판단 |
| [테스트결과_B_RAG적정성평가.md](테스트결과_B_RAG적정성평가.md) | 11~25번, RAG 적합/부적합 판정 로직 |
| [테스트결과_C_재료명정규화.md](테스트결과_C_재료명정규화.md) | 26~35번, 동의어·상위개념 정규화 |
| [테스트결과_D_레시피선택캐싱.md](테스트결과_D_레시피선택캐싱.md) | 36~43번, 옵션 선택·finalize 캐싱 |
| [테스트결과_E_예외엣지케이스.md](테스트결과_E_예외엣지케이스.md) | 44~50번, 예외/엣지 케이스 |
| [테스트결과_F_반복요청중복방지.md](테스트결과_F_반복요청중복방지.md) | 신규 기능 구현 + 91~100번, "다른 거 없어?" 반복 시 중복 방지 |

## 섹터별 요약

| 섹터 | 케이스 수 | 테스트 방식 | 결과 | 비고 |
|---|---|---|---|---|
| A. 재료 변경 판단 | 10 | 실제 LLM 호출 (노드 직접 호출, 그래프 전체 아님) | 9/10 완전일치, 1건(7번) 라벨링만 사소 차이 | 1번 "LLM 호출 없이"는 문서 오기 → 수정 |
| B. RAG 적정성 평가 | 15 | 순수 함수 호출 (`evaluate_rag_recipe`/`evaluate_rag_options`), LLM 없음 | 1차 5/15 불일치 → 문서의 임계값 가정 수정 후 재테스트 15/15 일치 | 실제 코드: 간이판정 N&lt;3, threshold 80%(N&lt;10)/70%(N≥10) |
| C. 재료명 정규화 | 10 | 순수 함수 호출 (`is_ingredient_satisfied`/`_merge_deduped`), LLM 없음 | 10/10 일치 | 수정 없음 |
| D. 레시피 선택 및 캐싱 | 8 | 실제 LLM 호출 (노드 직접 호출) | 1차 7/8 일치(39번 불일치) → 문서를 실제 프롬프트 설계 의도에 맞게 수정 | 코드 버그 아님, 문서 기대치 오류 |
| E. 예외·엣지 케이스 | 7 | 실제 LLM 호출 + 순수 함수 혼합 | 1차 5/7 일치(45, 50번 불일치) → 45번은 문서 수정, 50번은 **실제 기능 갭 확인 → 신규 기능 구현** | 50번이 F 섹터 신규 기능으로 이어짐 |
| F. 반복요청 중복방지 (신규) | 10 (×4라운드=40회) | 실제 RAG 검색 + 실제 LLM 호출, 라운드/케이스 간 5~10초 텀 | 40/40 라운드 전부 중복 없음 | 신규 기능: `shown_rag_ids`, `shown_titles` 상태 추가 |

## 수정 이력

| 항목 | 수정 전 | 수정 후 | 근거 |
|---|---|---|---|
| B 섹터 임계값 문서 | 간이판정 N&lt;6, threshold 60%/80% | 간이판정 N&lt;3, threshold 80%(N&lt;10)/70%(N≥10) | 코드(`nodes/preview_recipes.py`)와 15개 케이스 실측 대조 |
| D-39번 문서 | "LLM이 알아서 하나로 판단해 매칭" | "여러 개 동시 요청 시 무효 처리(재질문)가 정상" | `option_match_prompt`에 이미 명시된 규칙, 사용자 확인(옵션 1 선택) |
| E-45번 문서 | "재료 1개는 not_cookable" | "재료 1개는 needs_more_ingredients (reason 없음)" | 5회 반복 실행 모두 동일 결과 → 변동성 아닌 일관된 판정으로 확인 |
| E-50번 → 신규 기능 | (문서 없음, 코드 갭) | `shown_rag_ids`/`shown_titles` 상태 추가해 재요청 시 중복 방지 | 동일 재료 3회 연속 요청 시 제목이 거의 그대로 반복되는 걸 실측으로 확인 → 기능 구현 |

## 코드 변경 파일 (이번 테스트 사이클에서)

- [states.py](../states.py) — `shown_rag_ids`, `shown_titles` 필드 추가
- [nodes/nodes.py](../nodes/nodes.py) — `rag_adequacy_check`가 이미 보여준 RAG id 제외
- [nodes/preview_recipes.py](../nodes/preview_recipes.py) — `evaluate_rag_options`에 `exclude_ids` 파라미터, `preview_recipe_options`가 `shown_titles` 누적
